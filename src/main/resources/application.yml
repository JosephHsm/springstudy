spring:
  datasource:
    url: jdbc:mysql://localhost:3305/mydb  # DB ??, ?? ??? ?? mydb ??? ??
    username: root                         # DB ????
    password: root
    # DB ????
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # (create, update, validate, none ? ??)
    show-sql: true      # ???? SQL? ??