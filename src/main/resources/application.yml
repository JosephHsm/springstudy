spring:
  datasource:
    #url: jdbc:mysql://localhost:3305/mydb  # 노트북용 주소
    url: jdbc:mysql://localhost:3306/mydb
    username: root                         # DB ????
    #password: 1234 # 노트북 비번
    password: root

    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # (create, update, validate, none ? ??)
    show-sql: true      # ???? SQL? ??